import React from "react";
import images from '../images/pensil_logo.svg'
export default function Header(){
    return(
        <div className="hero">
            <img src={images} alt='' />

        </div>
    )

}